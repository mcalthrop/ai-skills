# CLI Flags

Always use long-form CLI flags (e.g. `--location` not `-L`, `--directory` not `-C`, `--verbose` not `-v`).

Commands should be self-documenting; short flags are opaque to readers unfamiliar with the tool. This applies to shell commands in code, skills, rules, and documentation.
